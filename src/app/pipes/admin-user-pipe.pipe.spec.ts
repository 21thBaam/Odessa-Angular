import { AdminUserPipePipe } from './admin-user-pipe.pipe';

describe('AdminUserPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AdminUserPipePipe();
    expect(pipe).toBeTruthy();
  });
});
